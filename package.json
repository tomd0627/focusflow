{
  "name": "focusflow",
  "version": "1.0.0",
  "description": "Pomodoro timer with Web Audio API ambient soundscapes",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "lint:js": "eslint js/",
    "lint:css": "stylelint \"css/**/*.css\"",
    "fix:js": "eslint --fix js/",
    "fix:css": "stylelint --fix \"css/**/*.css\"",
    "fix:format": "prettier --write ."
  },
  "lint-staged": {
    "*.html": "prettier --write",
    "*.js": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.css": [
      "prettier --write",
      "stylelint --fix"
    ]
  },
  "devDependencies": {
    "eslint": "^10.5.0",
    "globals": "^17.6.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.7",
    "prettier": "^3.8.4",
    "stylelint": "^17.13.0",
    "stylelint-config-standard": "^40.0.0",
    "stylelint-order": "^8.1.1"
  }
}
